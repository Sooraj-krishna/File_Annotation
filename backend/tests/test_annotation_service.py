"""
Tests for the AnnotationService CRUD operations.

Requires a live database connection. These tests validate the full
create → read → update → delete flow including coordinate validation
and error handling. The test database should be configured separately
from the development database.
"""




class TestAnnotationService:
    """
    Integration tests for AnnotationService.

    These tests require a database session fixture (to be added when
    test infrastructure is fully set up). For now, this class serves
    as a placeholder for future test development.
    """

    pass  # Integration tests require database — run via pytest-asyncio with test DB
